# Web Framework
fastapi==0.115.0
uvicorn==0.30.0

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# Environment & Configuration
python-dotenv==1.0.1
pydantic==2.9.0
pydantic-settings==2.5.0

# CORS (built into FastAPI, no extra package needed)

# File uploads (needed for POST /api/detect image upload)
python-multipart==0.0.9

# YOLO / Computer Vision (needed for server-side detection in Phase 5)
ultralytics==8.3.0
opencv-python-headless==4.10.0.84

# Testing
pytest==8.3.3
httpx==0.27.2

# Code Quality
black==24.8.0
